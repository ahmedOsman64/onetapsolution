# Flask Settings
FLASK_APP=app.py
FLASK_ENV=production
PORT=5000

# Security — Generate strong random keys!
# Run: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_THIS_TO_A_STRONG_RANDOM_KEY
JWT_SECRET_KEY=CHANGE_THIS_TO_ANOTHER_STRONG_RANDOM_KEY

# Database (MySQL / PlanetScale / Railway MySQL)
DB_HOST=your-database-host.com
DB_USER=your_db_user        # NOT root in production!
DB_PASSWORD=your_db_password
DB_NAME=otsDP
DB_PORT=3306

# Frontend URL for CORS (your Vercel URL)
FRONTEND_URL=https://your-app-name.vercel.app
